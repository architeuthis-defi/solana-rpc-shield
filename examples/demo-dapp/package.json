{
  "name": "solana-rpc-shield-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Phantom + devnet demo: wallet-signed transfers through the resilient pipeline, live health panel",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@solana/kit": "^6.9.0",
    "solana-rpc-shield": "file:../.."
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
